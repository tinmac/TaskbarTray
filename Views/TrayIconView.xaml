<?xml version="1.0" encoding="utf-8" ?>
<UserControl
    x:Class="TaskbarTray.Views.TrayIconView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converters="using:TaskbarTray.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:TaskbarTray.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:tb="using:H.NotifyIcon"
    mc:Ignorable="d">

    <UserControl.Resources>
        <converters:PowerModeToVisibilityConverter x:Key="PowerModeToVisibilityConverter" />

        <converters:BoolToImageSourceConverter
            x:Key="BoolToImageSourceConverter"
            FalseImage="/Assets/gauge_low.ico"
            TrueImage="/Assets/gauge_high.ico" />

    </UserControl.Resources>


    <!--
        Use new GUAGE FA icons
        - Find the heart icon used in I.Notify and use it her ewith FA??
        - IT WASNT in I.Notify, you founf it in a git hub quesiton on the H.NotifyIcon repo

        IconSource="{x:Bind IsWindowVisible, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, Converter={StaticResource BoolToImageSourceConverter}}"

        FontFamily="{StaticResource FA6_Light}"

        Guage Empty ie slow
        <FontIcon Foreground="Green" Glyph="&#f62d;" />

        Guage Full ie fast
        <FontIcon Foreground="Green" Glyph="&#f62d;" />

        RightClickCommand="{x:Bind RebuildMenuFlyoutCommand}"

    -->

    <tb:TaskbarIcon
        x:Name="TrayIcon"
        x:FieldModifier="public"
        ContextMenuMode="SecondWindow"
        IconSource="{x:Bind ViewModel.SelectedImage, Mode=OneWay}"
        IsRightTapEnabled="True"
       RightClickCommand="{x:Bind RebuildMenuFlyoutCommand}"
        LeftClickCommand="{x:Bind ViewModel.ToggleSpeedCommand}"
        NoLeftClickDelay="True">

        <!--<tb:TaskbarIcon.IconSource>
            <tb:GeneratedIconSource
                FontFamily="{StaticResource FA6_Light}"
                FontSize="32"
                Foreground="Gray"
                Text="&#xf2b9;" />
        </tb:TaskbarIcon.IconSource>-->

        <!--<tb:TaskbarIcon.IconSource>

            <tb:GeneratedIconSource
                FontFamily="{StaticResource FA6_Light}"
                Foreground="Gray"
                Text="\uf62d" />

        </tb:TaskbarIcon.IconSource>-->

        <!--<tb:TaskbarIcon.IconSource>
            <tb:GeneratedIconSource
                BorderThickness="1"
                FontSize="36"
                Text="❤️">
                <tb:GeneratedIconSource.Foreground>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="128,128">
                        <GradientStop Color="White" />
                        <GradientStop Color="Red" />
                    </LinearGradientBrush>
                </tb:GeneratedIconSource.Foreground>
                <tb:GeneratedIconSource.BorderBrush>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="128,128">
                        <GradientStop Color="White" />
                        <GradientStop Color="Red" />
                    </LinearGradientBrush>
                </tb:GeneratedIconSource.BorderBrush>
            </tb:GeneratedIconSource>
        </tb:TaskbarIcon.IconSource>-->

        <!--<tb:TaskbarIcon.ContextFlyout>

            <MenuFlyout
                x:Name="MyMenuFlyout"
                AreOpenCloseAnimationsEnabled="False">

                <MenuFlyoutItem Command="{x:Bind ExitApplicationCommand}" Text="Exit" />

                <MenuFlyoutSeparator />

                <MenuFlyoutItem Command="{x:Bind ViewModel.Set_HighPerformanceCommand}" Text="High Performance">
                    <MenuFlyoutItem.Icon>
                        <FontIcon
                            Margin="0 1 0 0"
                            FontFamily="{StaticResource FA6_Thin}"
                            FontSize="8"
                            Glyph="&#xf058;"
                            Visibility="{x:Bind ViewModel.IsHighVisible}" />
                    </MenuFlyoutItem.Icon>
                </MenuFlyoutItem>

                <MenuFlyoutItem Command="{x:Bind ViewModel.Set_BalancedCommand}" Text="Balanced">
                    <MenuFlyoutItem.Icon>

                        <FontIcon
                            Margin="0 1 0 0"
                            FontFamily="{StaticResource FA6_Thin}"
                            FontSize="8"
                            Glyph="&#xf006;"
                            Visibility="{x:Bind ViewModel.IsBalancedVisible}" />
                    </MenuFlyoutItem.Icon>
                </MenuFlyoutItem>

                <MenuFlyoutItem Command="{x:Bind ViewModel.Set_PowerSaverCommand}" Text="Power Saver">
                    <MenuFlyoutItem.Icon>

                        <FontIcon
                            Margin="0 1 0 0"
                            FontFamily="{StaticResource FA6_Thin}"
                            FontSize="8"
                            Glyph="&#xf058;"
                            Visibility="{x:Bind ViewModel.IsEcoVisible}" />

                    </MenuFlyoutItem.Icon>
                </MenuFlyoutItem>

                <MenuFlyoutSeparator />

                <MenuFlyoutItem Command="{x:Bind ViewModel.ShowHideWindowCommand}" Text="Show/Hide Window" />


            </MenuFlyout>

        </tb:TaskbarIcon.ContextFlyout>-->

    </tb:TaskbarIcon>

</UserControl>
